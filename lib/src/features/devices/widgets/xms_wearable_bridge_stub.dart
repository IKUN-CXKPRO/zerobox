import 'package:flutter/widgets.dart';

class XmsWearableBridge extends StatelessWidget {
  const XmsWearableBridge({required this.child, super.key});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
